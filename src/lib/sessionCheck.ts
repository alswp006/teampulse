// TDD red-phase placeholder — intentionally unimplemented.
// See src/__tests__/packet-0013.test.ts (packet 0013) for the full spec the
// Coder must implement here: call getIsTossLoginIntegratedService() exactly
// once, guarded by try/catch so a throw (no native bridge — WebView 밖) never
// escapes, resolving to its boolean result on success or false on failure.
export async function checkTossSession(): Promise<boolean> {
  return false;
}
